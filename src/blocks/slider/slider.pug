mixin slider(options)
  - const { value, min, max, stepsNumber, showBubble, showScale, color } = options;
  - const isBubbleVisible = showBubble ? "" : " slider__bubble_hide";
  - const isScaleVisible = showScale ? "" : " slider__scale_hide";
  - 
    const colors = {
      orange: "orange",
      green: "green"
    };
  - let i = 0;
  - let stepRange = max / (stepsNumber - 1)
  .slider.js-slider(data-value=value data-min=min data-max=max data-step=stepsNumber)
    div(class=`ui-slider-handle ui-slider-handle_${colors[color] || colors.orange}`)
      div(class=`slider__bubble js-slider__bubble${isBubbleVisible}`)
    div(class=`slider__scale${isScaleVisible}`)
      while i < stepsNumber
        span= i++ * stepRange