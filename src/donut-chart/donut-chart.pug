mixin donut-chart(items)
  svg.donut-chart(viewBox="0 0 32 32")
      each item in items
        circle.donut-chart__circle(r="16" cx="16" cy="16" stroke-width="32" stroke-dasharray=`${item.value} 100` stroke=`${item.color}`)
      circle.donut-chart__background(r="10" cx="16" cy="16")