mixin search(options)
  - const classes = options.emptyResult ? "search-form__input search-form__input_empty" : "search-form__input"
  form.search-form(action=options.action)
    input(class=classes, type="search", placeholder=options.placeholder, value=options.searchValue)
    button.search-form__submit(type="submit")