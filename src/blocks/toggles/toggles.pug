include ../title/title
include ../toggle/toggle

mixin toggles()
  section.toggles
    +title("Toggles")
    .toggles__row
      .toggles__item
        +toggle()
      .toggles__item
        +toggle("checked")